[pytest]
# Collect only the active test suite. This prevents a package-name collision
# with old_project_backup/tests (also named "tests"), which otherwise breaks
# bare `pytest` collection with "No module named 'tests.test_*'".
testpaths = tests
norecursedirs = old_project_backup .venv outputs .git
